import { describe, it, expect } from "vitest";

describe("Verificación de Entorno (Sanity Check)", () => {
  it("debería ejecutar Vitest correctamente", () => {
    expect(true).toBe(true);
  });
});
